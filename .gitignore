# NEVER commit game data or ISOs — patch (diff) only
*.iso
*.ISO
*.cpk
*.CPK
*.BIN
*.MTP
*.MXE
*.BF1
*.pmf
*.PMF
_work/

# the xdelta patch is large (movie re-encode) -> shipped as a Release asset,
# not committed to the repo
*.xdelta

# python bytecode
__pycache__/
*.pyc
patched_out/

# DLC drop-in package -> Release asset, not committed
*.zip
